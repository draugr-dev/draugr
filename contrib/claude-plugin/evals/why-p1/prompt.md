---
description: Priority comes from declared exposure and criticality, not from the finding
max_turns: 15
timeout_seconds: 600
allowed_tools: [Read, Glob, Grep]
---

report.json flags the same eval() call in two services. Why is it P1 in services/api but only P3 in services/web?
