project: python-pdm
release:
  version: "1.0"
config:
  controls:
    sca:
      enabled: true
      grypeFs:
        enabled: true
components:
  - name: python-pdm
    repositories:
      - url: ./python-pdm
