// Package saga defines the Draugr descriptor ("Saga") — the declarative account of an
// application's security surface (repositories, images, hosts, infrastructure) plus the
// controller configuration that drives a scan. It also handles parsing, validation,
// env-var substitution, and assembly of distributed meta-sources.
//
// See docs/ARCHITECTURE.md and docs/naming.md.
package saga
