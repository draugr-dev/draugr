{
  "$defs": {
    "component": {
      "additionalProperties": false,
      "description": "One logical part of the app. All surface lists are optional — declare what applies.",
      "properties": {
        "controllers": {
          "$ref": "#/$defs/controllers",
          "description": "Per-component control overrides. Same shape as config.controllers; component settings win."
        },
        "criticality": {
          "$ref": "#/$defs/criticality"
        },
        "exposure": {
          "$ref": "#/$defs/exposure"
        },
        "hosts": {
          "description": "Running endpoints — probed by headers, tls, and dast.",
          "items": {
            "$ref": "#/$defs/host"
          },
          "type": "array"
        },
        "images": {
          "description": "Container images — scanned by the images control.",
          "items": {
            "$ref": "#/$defs/image"
          },
          "type": "array"
        },
        "infrastructure": {
          "description": "Infrastructure this component runs on.",
          "items": {
            "$ref": "#/$defs/infrastructure"
          },
          "type": "array"
        },
        "labels": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Free-form key/value metadata, e.g. team or tier.",
          "type": "object"
        },
        "name": {
          "description": "Unique component name.",
          "type": "string"
        },
        "repositories": {
          "description": "Source repositories — scanned by sca, secrets, and sast.",
          "items": {
            "$ref": "#/$defs/repository"
          },
          "type": "array"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "config": {
      "additionalProperties": false,
      "description": "Project-wide configuration: which controls run, and what is reported where.",
      "properties": {
        "allowEffects": {
          "description": "Scanner effects the project accepts. A scanner that does more to a target than read it declares an effect; kinds that require consent (\"mutate\", \"privilege\") will not run unless listed here or allowed with --allow-effects.",
          "items": {
            "enum": [
              "mutate",
              "privilege",
              "network",
              "disclosure"
            ],
            "type": "string"
          },
          "type": "array",
          "uniqueItems": true
        },
        "controllers": {
          "$ref": "#/$defs/controllers"
        },
        "exclude": {
          "description": "Findings to suppress, each with a stated reason. A suppressed finding is still reported — marked, with its justification — it just stops counting toward the verdict.",
          "items": {
            "$ref": "#/$defs/excludeRule"
          },
          "type": "array"
        },
        "exploitability": {
          "$ref": "#/$defs/exploitabilityConfig"
        },
        "gate": {
          "$ref": "#/$defs/gateConfig"
        },
        "publishers": {
          "description": "Where rendered reports are delivered. Every report goes to every publisher.",
          "items": {
            "$ref": "#/$defs/publisherConfig"
          },
          "type": "array"
        },
        "reports": {
          "description": "Report formats to render for every scan.",
          "items": {
            "$ref": "#/$defs/reportConfig"
          },
          "type": "array"
        },
        "sbom": {
          "$ref": "#/$defs/sbomConfig"
        },
        "vex": {
          "$ref": "#/$defs/vexConfig"
        }
      },
      "type": "object"
    },
    "controlName": {
      "anyOf": [
        {
          "const": "dast",
          "description": "Probe a component's running endpoints for runtime vulnerabilities."
        },
        {
          "const": "headers",
          "description": "Check HTTP security headers on a component's running endpoints."
        },
        {
          "const": "iac",
          "description": "Scan Infrastructure-as-Code for insecure misconfigurations."
        },
        {
          "const": "images",
          "description": "Scan container images for known OS/package vulnerabilities."
        },
        {
          "const": "infrastructure",
          "description": "Audit a Kubernetes cluster against the CIS Kubernetes Benchmark."
        },
        {
          "const": "licenses",
          "description": "Report dependency licences that carry an obligation (copyleft, forbidden, unidentified)."
        },
        {
          "const": "sast",
          "description": "Static analysis of a repo's own source code for security bugs."
        },
        {
          "const": "sca",
          "description": "Scan a repo's dependencies for known vulnerabilities (Software Composition Analysis)."
        },
        {
          "const": "secrets",
          "description": "Detect committed secrets and credentials in a repo."
        },
        {
          "const": "threats",
          "description": "Check whether a component's hosts are known to threat-intelligence feeds."
        },
        {
          "const": "tls",
          "description": "Check TLS configuration and certificates on a component's running endpoints."
        }
      ],
      "description": "A control this build of Draugr implements. A name that is not here is rejected: a control Draugr cannot run is a check that will not happen, and a descriptor should not be able to ask for one quietly.",
      "type": "string"
    },
    "control_dast": {
      "additionalProperties": false,
      "description": "Probe a component's running endpoints for runtime vulnerabilities.",
      "properties": {
        "enabled": {
          "description": "Whether this control runs. An entry with no `enabled` key counts as enabled; an absent entry means disabled.",
          "type": "boolean"
        },
        "nuclei": {
          "additionalProperties": false,
          "description": "nuclei — published by projectdiscovery.",
          "properties": {
            "enabled": {
              "description": "Run this scanner. It is a default for this control, so it runs unless set to false.",
              "type": "boolean"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "control_headers": {
      "additionalProperties": false,
      "description": "Check HTTP security headers on a component's running endpoints.",
      "properties": {
        "draugrHeaders": {
          "additionalProperties": false,
          "description": "draugr-headers — published by draugr.",
          "properties": {
            "enabled": {
              "description": "Run this scanner. It is a default for this control, so it runs unless set to false.",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "enabled": {
          "description": "Whether this control runs. An entry with no `enabled` key counts as enabled; an absent entry means disabled.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "control_iac": {
      "additionalProperties": false,
      "description": "Scan Infrastructure-as-Code for insecure misconfigurations.",
      "properties": {
        "enabled": {
          "description": "Whether this control runs. An entry with no `enabled` key counts as enabled; an absent entry means disabled.",
          "type": "boolean"
        },
        "trivyConfig": {
          "additionalProperties": false,
          "description": "trivy-config — published by aquasecurity.",
          "properties": {
            "checks": {
              "description": "Paths to Rego check files, or directories of them, relative to where Draugr runs. Adds your own misconfiguration rules to Trivy's built-in ones.",
              "type": "array"
            },
            "enabled": {
              "description": "Run this scanner. It is a default for this control, so it runs unless set to false.",
              "type": "boolean"
            },
            "namespaces": {
              "description": "Rego namespaces to evaluate, e.g. [\"user\"]. Needed when your checks declare a namespace Trivy does not scan by default.",
              "type": "array"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "control_images": {
      "additionalProperties": false,
      "description": "Scan container images for known OS/package vulnerabilities.",
      "properties": {
        "enabled": {
          "description": "Whether this control runs. An entry with no `enabled` key counts as enabled; an absent entry means disabled.",
          "type": "boolean"
        },
        "grype": {
          "additionalProperties": false,
          "description": "grype — published by anchore.",
          "properties": {
            "byCve": {
              "description": "Report a finding under its CVE rather than the advisory ID the source used, where a CVE exists. On by default. Turn it off to see the advisory identifier Grype matched on.",
              "type": "boolean"
            },
            "enabled": {
              "description": "Run this scanner. It is opt-in, so it runs only when set to true.",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "trivy": {
          "additionalProperties": false,
          "description": "trivy — published by aquasecurity.",
          "properties": {
            "dbRepository": {
              "description": "OCI repositories to pull the vulnerability database from, in priority order. Point it at an internal mirror where the runner cannot reach a public registry.",
              "type": "array"
            },
            "enabled": {
              "description": "Run this scanner. It is a default for this control, so it runs unless set to false.",
              "type": "boolean"
            },
            "pkgTypes": {
              "description": "Which package types to analyse. Defaults to both. Narrow it to [\"library\"] when the OS layer is somebody else's responsibility — a base image maintained by a platform team — so the report covers what this component controls.",
              "items": {
                "enum": [
                  "os",
                  "library"
                ]
              },
              "type": "array"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "control_infrastructure": {
      "additionalProperties": false,
      "description": "Audit a Kubernetes cluster against the CIS Kubernetes Benchmark.",
      "properties": {
        "draugrK8sPolicies": {
          "additionalProperties": false,
          "description": "draugr-k8s-policies — published by draugr.",
          "properties": {
            "enabled": {
              "description": "Run this scanner. It is a default for this control, so it runs unless set to false.",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "enabled": {
          "description": "Whether this control runs. An entry with no `enabled` key counts as enabled; an absent entry means disabled.",
          "type": "boolean"
        },
        "kubeBench": {
          "additionalProperties": false,
          "description": "kube-bench — published by aquasecurity.",
          "properties": {
            "benchmark": {
              "description": "kube-bench benchmark to run, e.g. cis-1.9 or eks-1.5.0. Defaults to letting kube-bench detect the cluster's version.",
              "type": "string"
            },
            "configDir": {
              "description": "Path to kube-bench's cfg/ tree of benchmark definitions. Needed when the binary is on PATH and its cfg lives beside it rather than in /etc/kube-bench/cfg.",
              "type": "string"
            },
            "context": {
              "description": "kubeconfig context to run against. Defaults to the current context.",
              "type": "string"
            },
            "enabled": {
              "description": "Run this scanner. It is opt-in, so it runs only when set to true.",
              "type": "boolean"
            },
            "targets": {
              "description": "Comma-separated kube-bench targets to run, e.g. \"master,node\". Defaults to the node checks, which are what a scan from outside the control plane can answer.",
              "type": "string"
            },
            "version": {
              "description": "Kubernetes version to select the benchmark for, e.g. \"1.29\". Ignored when benchmark is set.",
              "type": "string"
            }
          },
          "type": "object"
        },
        "kubeBenchJob": {
          "additionalProperties": false,
          "description": "kube-bench-job — published by aquasecurity.",
          "properties": {
            "benchmark": {
              "description": "kube-bench benchmark to run, e.g. cis-1.9. Defaults to letting kube-bench detect the cluster's version.",
              "type": "string"
            },
            "context": {
              "description": "kubeconfig context to create the Job in. Defaults to the current context.",
              "type": "string"
            },
            "enabled": {
              "description": "Run this scanner. It is opt-in, so it runs only when set to true.",
              "type": "boolean"
            },
            "image": {
              "description": "kube-bench image the Job runs. Pin it to a digest for a reproducible benchmark, and to an internal mirror where the cluster cannot reach a public registry.",
              "type": "string"
            },
            "namespace": {
              "description": "Namespace to create the Job in. It must already exist; Draugr does not create namespaces.",
              "type": "string"
            },
            "nodeSelector": {
              "description": "Node selector for the Job, as comma-separated key=value pairs. Use it to benchmark a specific node pool.",
              "type": "string"
            },
            "targets": {
              "description": "Comma-separated kube-bench targets to run, e.g. \"master,node\".",
              "type": "string"
            },
            "timeout": {
              "description": "How long to wait for the Job to finish, e.g. \"5m\". Plain seconds are accepted.",
              "type": "string"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "control_licenses": {
      "additionalProperties": false,
      "description": "Report dependency licences that carry an obligation (copyleft, forbidden, unidentified).",
      "properties": {
        "enabled": {
          "description": "Whether this control runs. An entry with no `enabled` key counts as enabled; an absent entry means disabled.",
          "type": "boolean"
        },
        "mendLicenses": {
          "additionalProperties": false,
          "description": "mend-licenses — published by mend.io.",
          "properties": {
            "deny": {
              "description": "SPDX identifiers that fail the gate, e.g. [\"AGPL-3.0-only\", \"SSPL-1.0\"].",
              "type": "array"
            },
            "enabled": {
              "description": "Run this scanner. It is opt-in, so it runs only when set to true.",
              "type": "boolean"
            },
            "productToken": {
              "description": "The Mend product this component reports into. Required. It identifies a product and grants no access, so it belongs in the descriptor rather than the environment.",
              "type": "string"
            },
            "project": {
              "description": "Mend project name to report into. Defaults to the component's name, which keeps one project per component rather than per repository.",
              "type": "string"
            },
            "resultTimeout": {
              "description": "How long to wait for Mend to finish analysing an upload, e.g. \"15m\". Raise it for large monorepos, where the agent's upload finishes long before the analysis does.",
              "type": "string"
            },
            "settings": {
              "description": "Unified Agent configuration keys, passed through verbatim. Draugr writes the component's scope exclusions first, so anything here overrides them.",
              "type": "object"
            },
            "warn": {
              "description": "SPDX identifiers reported as warnings rather than failures, e.g. [\"GPL-3.0-only\"].",
              "type": "array"
            }
          },
          "type": "object"
        },
        "trivyLicense": {
          "additionalProperties": false,
          "description": "trivy-license — published by aquasecurity.",
          "properties": {
            "deny": {
              "description": "SPDX identifiers that fail the gate, e.g. [\"AGPL-3.0-only\", \"SSPL-1.0\"].",
              "type": "array"
            },
            "enabled": {
              "description": "Run this scanner. It is a default for this control, so it runs unless set to false.",
              "type": "boolean"
            },
            "warn": {
              "description": "SPDX identifiers reported as warnings rather than failures, e.g. [\"GPL-3.0-only\"].",
              "type": "array"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "control_sast": {
      "additionalProperties": false,
      "description": "Static analysis of a repo's own source code for security bugs.",
      "properties": {
        "enabled": {
          "description": "Whether this control runs. An entry with no `enabled` key counts as enabled; an absent entry means disabled.",
          "type": "boolean"
        },
        "gosec": {
          "additionalProperties": false,
          "description": "gosec — published by securego.",
          "properties": {
            "enabled": {
              "description": "Run this scanner. It is opt-in, so it runs only when set to true.",
              "type": "boolean"
            },
            "exclude": {
              "description": "Skip these gosec rules, by ID, e.g. [\"G104\"]. For rules that do not apply to the codebase — a finding you have judged and accepted belongs in exclusions, where it stays in the report marked suppressed.",
              "type": "array"
            },
            "include": {
              "description": "Run only these gosec rules, by ID, e.g. [\"G101\", \"G204\"]. Everything else is skipped.",
              "type": "array"
            },
            "tags": {
              "description": "Go build tags to compile with, e.g. [\"integration\"]. Code behind a tag gosec does not build is code it does not analyse.",
              "type": "array"
            }
          },
          "type": "object"
        },
        "semgrep": {
          "additionalProperties": false,
          "description": "semgrep — published by semgrep.",
          "properties": {
            "config": {
              "description": "Ruleset to run: a Semgrep registry ref (e.g. p/owasp-top-ten) or a path/URL to a rules file. Defaults to p/default.",
              "type": "string"
            },
            "enabled": {
              "description": "Run this scanner. It is a default for this control, so it runs unless set to false.",
              "type": "boolean"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "control_sca": {
      "additionalProperties": false,
      "description": "Scan a repo's dependencies for known vulnerabilities (Software Composition Analysis).",
      "properties": {
        "enabled": {
          "description": "Whether this control runs. An entry with no `enabled` key counts as enabled; an absent entry means disabled.",
          "type": "boolean"
        },
        "grypeFs": {
          "additionalProperties": false,
          "description": "grype-fs — published by anchore.",
          "properties": {
            "byCve": {
              "description": "Report a finding under its CVE rather than the advisory ID the source used, where a CVE exists. On by default. Turn it off to see the advisory identifier Grype matched on.",
              "type": "boolean"
            },
            "enabled": {
              "description": "Run this scanner. It is opt-in, so it runs only when set to true.",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "mendSca": {
          "additionalProperties": false,
          "description": "mend-sca — published by mend.io.",
          "properties": {
            "enabled": {
              "description": "Run this scanner. It is opt-in, so it runs only when set to true.",
              "type": "boolean"
            },
            "productToken": {
              "description": "The Mend product this component reports into. Required. It identifies a product and grants no access, so it belongs in the descriptor rather than the environment.",
              "type": "string"
            },
            "project": {
              "description": "Mend project name to report into. Defaults to the component's name, which keeps one project per component rather than per repository.",
              "type": "string"
            },
            "resultTimeout": {
              "description": "How long to wait for Mend to finish analysing an upload, e.g. \"15m\". Raise it for large monorepos, where the agent's upload finishes long before the analysis does.",
              "type": "string"
            },
            "settings": {
              "description": "Unified Agent configuration keys, passed through verbatim. Draugr writes the component's scope exclusions first, so anything here overrides them.",
              "type": "object"
            }
          },
          "type": "object"
        },
        "trivyFs": {
          "additionalProperties": false,
          "description": "trivy-fs — published by aquasecurity.",
          "properties": {
            "dbRepository": {
              "description": "OCI repositories to pull the vulnerability database from, in priority order. Point it at an internal mirror where the runner cannot reach a public registry.",
              "type": "array"
            },
            "enabled": {
              "description": "Run this scanner. It is a default for this control, so it runs unless set to false.",
              "type": "boolean"
            },
            "pkgTypes": {
              "description": "Which package types to analyse. Defaults to both. Narrow it to [\"library\"] when the OS layer is somebody else's responsibility — a base image maintained by a platform team — so the report covers what this component controls.",
              "items": {
                "enum": [
                  "os",
                  "library"
                ]
              },
              "type": "array"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "control_secrets": {
      "additionalProperties": false,
      "description": "Detect committed secrets and credentials in a repo.",
      "properties": {
        "enabled": {
          "description": "Whether this control runs. An entry with no `enabled` key counts as enabled; an absent entry means disabled.",
          "type": "boolean"
        },
        "gitleaks": {
          "additionalProperties": false,
          "description": "gitleaks — published by gitleaks.",
          "properties": {
            "config": {
              "description": "Path to a Gitleaks TOML rules file, relative to where Draugr runs. Use it for a ruleset shared across repositories; a .gitleaks.toml committed in the scanned repository is already picked up without this.",
              "type": "string"
            },
            "enabled": {
              "description": "Run this scanner. It is a default for this control, so it runs unless set to false.",
              "type": "boolean"
            },
            "history": {
              "description": "Also scan the repository's commit history, in addition to the working tree. Off by default: it needs a full clone rather than a shallow one, so it is slower on a large repository. A secret committed and later removed is still fetchable by anyone who can clone, so it is still compromised — this is what finds it. History findings are marked as such, because they name the path the secret had when it was introduced.",
              "type": "boolean"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "control_threats": {
      "additionalProperties": false,
      "description": "Check whether a component's hosts are known to threat-intelligence feeds.",
      "properties": {
        "enabled": {
          "description": "Whether this control runs. An entry with no `enabled` key counts as enabled; an absent entry means disabled.",
          "type": "boolean"
        },
        "urlhaus": {
          "additionalProperties": false,
          "description": "urlhaus — published by abuse.ch.",
          "properties": {
            "enabled": {
              "description": "Run this scanner. It is a default for this control, so it runs unless set to false.",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "virustotal": {
          "additionalProperties": false,
          "description": "virustotal — published by VirusTotal (Google).",
          "properties": {
            "enabled": {
              "description": "Run this scanner. It is opt-in, so it runs only when set to true.",
              "type": "boolean"
            },
            "requestsPerMinute": {
              "description": "Override the assumed API allowance. Defaults to the public tier's 4. Raise it only to what your key actually permits — exceeding VirusTotal's limit risks losing access, and their stated penalty for terms violations is a permanent ban.",
              "type": "integer"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "control_tls": {
      "additionalProperties": false,
      "description": "Check TLS configuration and certificates on a component's running endpoints.",
      "properties": {
        "draugrTls": {
          "additionalProperties": false,
          "description": "draugr-tls — published by draugr.",
          "properties": {
            "enabled": {
              "description": "Run this scanner. It is a default for this control, so it runs unless set to false.",
              "type": "boolean"
            },
            "expiryErrorDays": {
              "description": "Report an error when the certificate expires within this many days. Defaults to 14.",
              "type": "integer"
            },
            "expiryWarnDays": {
              "description": "Report a warning when the certificate expires within this many days. Defaults to 30. Lower it for endpoints with automated renewal so the gate only fires when renewal has failed.",
              "type": "integer"
            }
          },
          "type": "object"
        },
        "enabled": {
          "description": "Whether this control runs. An entry with no `enabled` key counts as enabled; an absent entry means disabled.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "controllerSettings": {
      "additionalProperties": {
        "description": "Per-scanner configuration. `enabled` turns a scanner on or off; the remaining keys are that scanner's own options, validated against its schema when the scan runs.",
        "properties": {
          "enabled": {
            "description": "Run this scanner. Default scanners run unless set to false; opt-in scanners run only when set to true.",
            "type": "boolean"
          }
        },
        "type": "object"
      },
      "description": "Settings for one control: `enabled`, plus a block per scanner.",
      "properties": {
        "enabled": {
          "description": "Whether this control runs. An entry with no `enabled` key counts as enabled; an absent entry means disabled.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "controllers": {
      "additionalProperties": {
        "$ref": "#/$defs/controllerSettings"
      },
      "description": "Security controls, keyed by name. A control runs only when enabled here (or on a component). Each scanner is configured under its own key inside the control.",
      "properties": {
        "dast": {
          "$ref": "#/$defs/control_dast"
        },
        "headers": {
          "$ref": "#/$defs/control_headers"
        },
        "iac": {
          "$ref": "#/$defs/control_iac"
        },
        "images": {
          "$ref": "#/$defs/control_images"
        },
        "infrastructure": {
          "$ref": "#/$defs/control_infrastructure"
        },
        "licenses": {
          "$ref": "#/$defs/control_licenses"
        },
        "sast": {
          "$ref": "#/$defs/control_sast"
        },
        "sca": {
          "$ref": "#/$defs/control_sca"
        },
        "secrets": {
          "$ref": "#/$defs/control_secrets"
        },
        "threats": {
          "$ref": "#/$defs/control_threats"
        },
        "tls": {
          "$ref": "#/$defs/control_tls"
        }
      },
      "propertyNames": {
        "$ref": "#/$defs/controlName"
      },
      "type": "object"
    },
    "criticality": {
      "anyOf": [
        {
          "const": "critical",
          "description": "Failure causes an outage or data loss."
        },
        {
          "const": "important",
          "description": "Degraded functionality, no immediate outage."
        },
        {
          "const": "supporting",
          "description": "Limited operational impact."
        }
      ],
      "description": "Business impact if this component fails — the impact axis of risk prioritization.",
      "type": "string"
    },
    "excludeRule": {
      "additionalProperties": false,
      "description": "One exclusion. Set paths, rules, or both; when both are set a finding must match both.",
      "properties": {
        "acceptedBy": {
          "description": "Who decided this finding was acceptable. Optional; a suppression without one is reported as unattributed.",
          "minLength": 1,
          "type": "string"
        },
        "expires": {
          "description": "Date this exclusion stops applying (YYYY-MM-DD). Past it the finding returns and the report says the exclusion lapsed.",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "type": "string"
        },
        "paths": {
          "description": "Location patterns. A pattern ending in '/' matches everything beneath that directory; otherwise it is a glob against the whole location.",
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "reason": {
          "description": "Why this exclusion exists. Required: an exclusion without a reason is indistinguishable from an oversight.",
          "minLength": 1,
          "type": "string"
        },
        "rules": {
          "description": "Rule ids to suppress, matched exactly.",
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "vex": {
          "$ref": "#/$defs/vexDecision"
        }
      },
      "required": [
        "reason"
      ],
      "type": "object"
    },
    "exploitabilityConfig": {
      "additionalProperties": false,
      "description": "Severity enrichment from real-world exploitability signals: CISA's Known Exploited Vulnerabilities catalog and FIRST's EPSS scores. A CVE on KEV becomes critical; a CVE at or above the EPSS threshold is raised one band. Applied before ranking, so priority reflects what is being exploited rather than only what could be. Either signal works without the other. Fetch the data with `draugr feeds update`.",
      "properties": {
        "epss": {
          "description": "Where the EPSS scores come from: a file path, \"cache\", or \"auto\" — the same three choices as kev. Omit to leave EPSS off.",
          "examples": [
            "cache",
            "auto",
            "./epss.csv"
          ],
          "type": "string"
        },
        "epssThreshold": {
          "description": "EPSS probability at or above which a finding's severity is raised one band. Defaults to 0.5 — a coin-flip chance of exploitation within 30 days. Scores are heavily skewed, so lowering this widens the net faster than the number suggests. Zero disables the EPSS bump while leaving KEV in force.",
          "maximum": 1,
          "minimum": 0,
          "type": "number"
        },
        "kev": {
          "description": "Where the KEV catalog comes from: a file path, \"cache\" to read ~/.draugr/feeds without touching the network, or \"auto\" to fetch when the cache is missing or stale. Omit to leave KEV off.",
          "examples": [
            "cache",
            "auto",
            "./kev.json"
          ],
          "type": "string"
        },
        "maxAge": {
          "description": "How old a cached feed may be before \"auto\" refetches it and a scan warns that it is stale. A Go duration; defaults to 24h, tracking EPSS being republished daily. Raise it on a runner deliberately pinned to a known copy of the data.",
          "examples": [
            "24h",
            "168h"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "exposure": {
      "anyOf": [
        {
          "const": "public",
          "description": "Internet-facing, no authentication."
        },
        {
          "const": "authenticated",
          "description": "Internet-facing, behind authentication."
        },
        {
          "const": "internal",
          "description": "Reachable within the environment."
        },
        {
          "const": "restricted",
          "description": "Namespace- or network-policy-scoped."
        }
      ],
      "description": "How reachable this component is — the likelihood axis of risk prioritization.",
      "type": "string"
    },
    "fragmentConfig": {
      "additionalProperties": false,
      "description": "The part of config a fragment may set.",
      "properties": {
        "exclude": {
          "description": "Suppressions this fragment contributes, appended to the descriptor's own.",
          "items": {
            "$ref": "#/$defs/excludeRule"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "fragmentRef": {
      "additionalProperties": false,
      "description": "Saga fragments to merge in. A fragment adds components and exclusions; it cannot change policy, so including one can never lower the gate or turn a control off.",
      "properties": {
        "path": {
          "description": "Which fragment files to read. Globs use the same dialect as paths and ignore — * within a segment, ** across them. Relative to the file that names it.",
          "minLength": 1,
          "type": "string"
        },
        "revision": {
          "description": "Branch, tag or commit to read. Required when url is set, and not defaulted: a fragment tracking a moving branch is a gate that changes with no commit here.",
          "minLength": 1,
          "type": "string"
        },
        "url": {
          "description": "Git repository to read the fragments from. Omit for a local path.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    },
    "gateConfig": {
      "additionalProperties": false,
      "description": "Pass/fail thresholds. Policy belongs in the descriptor rather than in a flag every pipeline has to remember.",
      "properties": {
        "controls": {
          "additionalProperties": {
            "enum": [
              "error",
              "warning",
              "note"
            ],
            "type": "string"
          },
          "description": "Per-control severity threshold, overriding --fail-on for that control only.",
          "propertyNames": {
            "$ref": "#/$defs/controlName"
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "host": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "description": "Label for this endpoint.",
          "type": "string"
        },
        "type": {
          "anyOf": [
            {
              "const": "browser",
              "description": "Served to browsers — full security-header suite (default)."
            },
            {
              "const": "api",
              "description": "API endpoint — skips browser-only headers."
            }
          ],
          "description": "Tunes which header checks apply.",
          "type": "string"
        },
        "url": {
          "description": "Endpoint URL. Must be https:// for the tls control.",
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    },
    "image": {
      "additionalProperties": false,
      "properties": {
        "digest": {
          "description": "Pin the immutable content digest, e.g. sha256:…",
          "type": "string"
        },
        "image": {
          "description": "Image reference, e.g. registry.example.com/acme/web:1.0",
          "type": "string"
        }
      },
      "required": [
        "image"
      ],
      "type": "object"
    },
    "infrastructure": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "description": "Infrastructure type, e.g. kubernetes.",
          "type": "string"
        },
        "namespaces": {
          "description": "Namespaces this component owns. Omit to audit the whole cluster. Only the draugrK8sPolicies scanner can honour a scope; the others refuse rather than silently report the whole cluster.",
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array",
          "uniqueItems": true
        },
        "ref": {
          "description": "Identifier, e.g. a cluster name.",
          "type": "string"
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "publisherConfig": {
      "additionalProperties": false,
      "properties": {
        "commit": {
          "description": "github: commit SHA. Defaults to $GITHUB_SHA.",
          "type": "string"
        },
        "dir": {
          "description": "file: output directory.",
          "type": "string"
        },
        "kind": {
          "anyOf": [
            {
              "const": "file",
              "description": "Write each report into a directory."
            },
            {
              "const": "github",
              "description": "Upload the SARIF report to GitHub code scanning."
            },
            {
              "const": "github-pr-comment",
              "description": "Post the Markdown report as a sticky pull-request comment."
            },
            {
              "const": "azure-pr-comment",
              "description": "Post the Markdown report as a sticky Azure DevOps pull-request comment."
            },
            {
              "const": "gitlab-mr-comment",
              "description": "Post the Markdown report as a sticky GitLab merge-request comment."
            }
          ],
          "description": "Where to deliver reports.",
          "type": "string"
        },
        "marker": {
          "description": "github-pr-comment / azure-pr-comment / gitlab-mr-comment: hidden marker identifying the sticky comment to update.",
          "type": "string"
        },
        "org": {
          "description": "azure-pr-comment: collection URI. Defaults to $SYSTEM_TEAMFOUNDATIONCOLLECTIONURI.",
          "type": "string"
        },
        "pr": {
          "description": "github-pr-comment / azure-pr-comment: pull-request number; gitlab-mr-comment: merge-request IID. Defaults to the CI environment.",
          "type": "integer"
        },
        "project": {
          "description": "azure-pr-comment: team project. Defaults to $SYSTEM_TEAMPROJECT.",
          "type": "string"
        },
        "ref": {
          "description": "github: git ref. Defaults to $GITHUB_REF.",
          "type": "string"
        },
        "repo": {
          "description": "github: owner/name (default $GITHUB_REPOSITORY). azure-pr-comment: repository name (default $BUILD_REPOSITORY_NAME). gitlab-mr-comment: project id or full path (default $CI_PROJECT_ID).",
          "type": "string"
        },
        "tokenEnv": {
          "description": "Environment variable holding the token (default GITHUB_TOKEN; SYSTEM_ACCESSTOKEN for azure-pr-comment; GITLAB_TOKEN for gitlab-mr-comment). The token itself is never stored in the Saga.",
          "type": "string"
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "reference": {
      "additionalProperties": false,
      "properties": {
        "link": {
          "description": "URL to the document.",
          "type": "string"
        },
        "type": {
          "description": "Kind of reference, e.g. ThreatModel.",
          "type": "string"
        }
      },
      "required": [
        "type",
        "link"
      ],
      "type": "object"
    },
    "release": {
      "additionalProperties": false,
      "description": "Identifies what is being assessed.",
      "properties": {
        "name": {
          "description": "Release or application name.",
          "type": "string"
        },
        "stage": {
          "description": "Free-form stage label, e.g. dev, staging, prod.",
          "type": "string"
        },
        "version": {
          "description": "Release version. Supports ${{ ENV_VAR }} substitution.",
          "type": "string"
        }
      },
      "required": [
        "version"
      ],
      "type": "object"
    },
    "reportConfig": {
      "additionalProperties": false,
      "properties": {
        "filename": {
          "description": "Override the artifact's default output filename.",
          "type": "string"
        },
        "format": {
          "description": "Report format to render.",
          "enum": [
            "console",
            "gitlab-codequality",
            "gitlab-cyclonedx",
            "gitlab-dependency-scanning",
            "gitlab-sast",
            "gitlab-secret-detection",
            "html",
            "json",
            "junit",
            "markdown",
            "sarif",
            "template",
            "vex"
          ],
          "type": "string"
        },
        "minPriority": {
          "description": "Narrow this report to findings at or above a priority band, leaving every other report complete. The artifact states the band it was narrowed to.",
          "enum": [
            "P1",
            "P2",
            "P3",
            "P4"
          ],
          "type": "string"
        },
        "template": {
          "description": "Inline Go text/template (format: template). Set exactly one of template / templateFile.",
          "type": "string"
        },
        "templateFile": {
          "description": "Path to a Go text/template (format: template).",
          "type": "string"
        }
      },
      "required": [
        "format"
      ],
      "type": "object"
    },
    "repository": {
      "additionalProperties": false,
      "properties": {
        "ignore": {
          "description": "Remove matching paths from the scan, applied after `paths`. Gitignore-style: a trailing `/` is a directory, `*` matches within a path segment, `**` across them.",
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "paths": {
          "description": "Restrict the scan to these directories. Files at the repository root are always included — manifests and scanner configuration live there.",
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        },
        "revision": {
          "description": "Branch, tag, or commit to scan.",
          "type": "string"
        },
        "url": {
          "description": "Repository URL, or a local path such as `.`",
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    },
    "sbomConfig": {
      "additionalProperties": false,
      "description": "Software Bill of Materials generation. Evidence rather than a control: an SBOM is an inventory of what a component contains, so it finds nothing and never affects the verdict. One document is produced per distinct repository and image. Requires the syft tool.",
      "properties": {
        "enabled": {
          "description": "Generate an SBOM for every repository and image in the project.",
          "type": "boolean"
        },
        "format": {
          "description": "SBOM document format. Defaults to cyclonedx-json. CycloneDX and SPDX are each offered in both of their standard encodings; pick whichever your consumer reads.",
          "enum": [
            "cyclonedx-json",
            "cyclonedx-xml",
            "spdx-json",
            "spdx-tag-value"
          ],
          "type": "string"
        },
        "scope": {
          "description": "What each document covers. component (default) is one per repository and image; project is one covering the whole release, assembled along the hierarchy the Saga declares; both emits the parts and the whole. project and both require format cyclonedx-json.",
          "enum": [
            "component",
            "project",
            "both"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "vexConfig": {
      "additionalProperties": false,
      "description": "Who is making the claims in a generated VEX document, and what product they are about. Both optional and both worth setting for a document you publish.",
      "properties": {
        "author": {
          "description": "Who is making these statements — an organisation, ideally with a contact. Defaults to the release name, which is a project rather than a party.",
          "minLength": 1,
          "type": "string"
        },
        "product": {
          "description": "The product the statements are about, as an IRI; a package URL is conventional. Defaults to a purl over the release. Set it to whatever the SBOM your consumer holds calls this product, or nothing will match.",
          "minLength": 1,
          "type": "string"
        }
      },
      "type": "object"
    },
    "vexDecision": {
      "additionalProperties": false,
      "description": "What this suppression claims in VEX terms, for `--report vex`. Optional: without it the finding is reported as affected, the reading that is never an overstatement.",
      "properties": {
        "justification": {
          "description": "Why the product is not affected, from VEX's fixed vocabulary. Valid only with status not_affected; without it the exclusion's reason is emitted as prose.",
          "enum": [
            "component_not_present",
            "vulnerable_code_not_present",
            "vulnerable_code_not_in_execute_path",
            "vulnerable_code_cannot_be_controlled_by_adversary",
            "inline_mitigations_already_exist"
          ],
          "type": "string"
        },
        "status": {
          "description": "The claim. under_investigation is not accepted here: it is what an untriaged finding already reports, and a finding you have suppressed is one you have finished investigating.",
          "enum": [
            "not_affected",
            "affected",
            "fixed"
          ],
          "type": "string"
        }
      },
      "required": [
        "status"
      ],
      "type": "object"
    }
  },
  "$id": "https://draugr.dev/schema/draugr.saga.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "The draugr.saga.yaml descriptor — a declarative account of an application's security surface and the controls that must pass. See https://draugr.dev/docs/latest/reference/saga-schema/",
  "properties": {
    "components": {
      "description": "The app's parts. Each component declares the surfaces it exposes; Draugr plans scans per surface.",
      "items": {
        "$ref": "#/$defs/component"
      },
      "type": "array"
    },
    "config": {
      "$ref": "#/$defs/config"
    },
    "fragments": {
      "description": "Saga fragments to merge into this descriptor.",
      "items": {
        "$ref": "#/$defs/fragmentRef"
      },
      "type": "array"
    },
    "references": {
      "description": "Links to manual or human-performed controls (threat model, architecture diagram, …).",
      "items": {
        "$ref": "#/$defs/reference"
      },
      "type": "array"
    },
    "release": {
      "$ref": "#/$defs/release"
    }
  },
  "required": [
    "release"
  ],
  "title": "Draugr Saga",
  "type": "object"
}
