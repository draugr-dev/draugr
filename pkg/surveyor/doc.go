// Package surveyor is the Raven framework — discovery plugins that inspect an
// environment (a Kubernetes cluster, a GitHub org, an ADO project) and return Saga
// fragments so the descriptor can write itself.
//
// See docs/ARCHITECTURE.md and docs/naming.md.
package surveyor
