// Package plugin is the Draugr plugin SDK. It defines the three extension kinds —
// Scanner, Controller, and Surveyor — the shared value types (Target, Config), and the
// transports (in-process built-ins, gRPC out-of-process plugins, and declarative tool
// adapters).
//
// See docs/plugin-api.md.
package plugin
