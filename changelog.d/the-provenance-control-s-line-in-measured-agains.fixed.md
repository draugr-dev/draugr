The provenance control's line in `MEASURED AGAINST` says the same thing whatever size the descriptor is. It listed every signer and every identity it observed, and that block gives a control three lines, so a project with six signers saw three names and a severed URL where the rest had been. It now reports counts, names the first few signers, and `--format json` carries every identity in full.
