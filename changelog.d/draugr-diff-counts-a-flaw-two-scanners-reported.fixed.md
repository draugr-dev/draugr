`draugr diff` counts a flaw two scanners reported once, the way `draugr scan` already did. A pull request adding a library that a manifest scanner and a file scanner both report was described as fourteen new findings in the comment and nine in the terminal; both numbers were about the same change. The console row now also names the other scanner, as the scan report does. The diff's SARIF is unchanged and still carries what each scanner found.
